# 交付说明 · WorkBuddy Android App

## 当前状态

| 事项 | 状态 |
|------|------|
| 移动端网页问题排查（登录遮挡 / 标题未居中） | ✅ 已完成 |
| Android 封装 App 开发（WebView + CSS 注入修复） | ✅ 已完成 |
| 编译 Release 签名 APK | ✅ 已完成 |
| APK 图标使用 WorkBuddy Logo | ✅ 已完成 |
| 发布到 GitHub Release v1.1.0 | ✅ 已完成 |
| 更新 README 文档 | ✅ 已完成 |

## 修复内容（v1.1）

- **问题**：`https://www.workbuddy.cn/app` 的桌面布局固定 540px，在手机视口（390px）下右侧被切掉约 134px，导致登录按钮被遮挡、标题不居中
- **方案**：`MainActivity` 在 `onPageFinished` 时通过 JS 注入 `width=device-width` 的 viewport meta + 187 行移动端适配 CSS
- **效果**：390/375/412px 视口下 `scrollWidth == clientWidth`，无横向溢出

## 安装测试

直接下载 GitHub Release 中的 `WorkBuddy-v1.1-release.apk` 在安卓手机上安装即可。

## App 技术参数

- 包名：`com.workbuddy.app` ｜ 起始页：`https://www.workbuddy.cn/app`
- minSdk 21 ｜ targetSdk / compileSdk 35
- 已启用：JavaScript、DOM/数据库存储、下拉刷新、返回键网页后退、UA 追加 `WorkBuddyApp/1.0`
- 图标：WorkBuddy 官方 Logo 渲染为 mdpi→xxxhdpi 各分辨率

## ⚠️ 安全提醒

- `workbuddy.keystore` 为临时签名密钥，**正式发布前请替换为自有密钥**
- 请勿将任何 Token 明文发送到聊天中