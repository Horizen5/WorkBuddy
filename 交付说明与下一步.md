# 交付说明 · WorkBuddy Android App

## 当前状态

| 事项 | 状态 |
|------|------|
| 移动端网页问题排查（登录遮挡 / 标题未居中） | ✅ 已完成 |
| Android 封装 App 开发（WebView + CSS 注入修复） | ✅ 已完成 |
| 编译 Release 签名 APK | ✅ 已完成 |
| APK 图标使用 WorkBuddy Logo | ✅ 已完成 |
| 发布到 GitHub Release v1.4.0 | ✅ 已完成（Release 需手动创建，API 受限） |
| 更新 README 文档 | ✅ 已完成 |
| 状态栏暗色磨砂底（浅色页面可见） | ✅ 已完成 |
| 网页文件上传调用安卓文件管理器 | ✅ 已完成 |
| 状态栏与顶栏相反色、内容不再贴顶 | ✅ 已完成 |
| 右上角刷新按钮（登录后显示） | ✅ 已完成 |
| 文件选择器卡死/黑屏修复 | ✅ 已完成 |
| 下拉刷新防误触（仅顶部可刷新） | ✅ 已完成 |

## 修复内容（v1.4.0）

- **状态栏与顶栏相反色**：状态栏改为深色实色（`0xFF0F1419`）+ 浅色图标，页面顶栏为浅色实底，二者对比清晰；不再把软件顶部染成黑灰
- **内容不再贴顶**：内容不再延伸到状态栏下方，顶栏增加固定上间距，避免内容顶到屏幕最上沿
- **右上角刷新按钮（登录后显示）**：通过 JS 桥接检测登录态，未登录 / 加载中隐藏，登录后右上角显示刷新按钮
- **文件选择器卡死 / 黑屏**：调起系统文件管理器前禁用下拉刷新手势，补充 `CATEGORY_OPENABLE`，失败时回退 `ACTION_GET_CONTENT`，结果回调稳妥收尾
- **下拉被误判为刷新**：仅在页面滚动到顶部时启用下拉刷新，已下滑时下拉用于浏览内容

## 修复内容（v1.3.0）

- **状态栏/导航条看不清**：浅色页面下透明状态栏导致系统图标（时间、电量等）与背景糊在一起；改为深色半透明底（`0xB3000000`）并强制浅色系统图标
- **文件上传无响应**：网页 `<input type="file">` 原先无处理；现通过 `WebChromeClient.onShowFileChooser` 调起 Android 原生文件选择器
- **页面空白/不协调**：body 改为浅灰底色、主面板加圆角轻阴影；取消主内容区过度垂直居中，改为自然拉伸排列，减少空洞感

## 安装测试

直接下载 GitHub Release 中的 `WorkBuddy-v1.4-release.apk` 在安卓手机上安装即可。

## App 技术参数

- 包名：`com.workbuddy.app` ｜ 起始页：`https://www.workbuddy.cn/app`
- minSdk 21 ｜ targetSdk / compileSdk 35
- 已启用：JavaScript、DOM/数据库存储、下拉刷新、返回键网页后退、UA 追加 `WorkBuddyApp/1.4.0`
- 图标：WorkBuddy 官方 Logo 渲染为 mdpi→xxxhdpi 各分辨率

## ⚠️ 安全提醒

- `workbuddy.keystore` 为临时签名密钥，**正式发布前请替换为自有密钥**
- 请勿将任何 Token 明文发送到聊天中